# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## What this is

An object-oriented game engine for Roku, written in [BrighterScript](https://github.com/rokucommunity/brighterscript) (a typed superset of BrightScript that compiles to `.brs`). Distributed via ROPM. All engine source lives under `src/source/` inside the `BGE` namespace; `examples/` contains full sample Roku channels (asteroids, breakout, pong, snake, 3d, canvas, hybrid, pixels, quickstart, terrain) that consume the engine via ROPM and serve as the main way to exercise engine changes end-to-end, plus `rendererTest` — a categorized, menu-driven suite of `BGE.Renderer` demos, deliberately built without `BGE.Game`/`Room` (see its own note under [Architecture](#architecture) below). There is also a growing Rooibos unit test suite (colocated `*.spec.bs` files) — see [Unit tests](#unit-tests-rooibos) below.

## Commands

Run from the repo root unless noted.

- `npm run build` — clean, then compile the engine with `bsc` (output to `build/`)
- `npm run validate` — type-checks the engine both without tests (`bsconfig.build.json`) and with tests (`bsconfig.test.json`) — always run it after changing engine code.
- `npm run lint` — run `bslint` (rules configured in `bslint.json`)
- `npm run check` — run the standard local quality gate: lint, validate, and headless tests
- `npm run check:all` — run the standard quality gate, then validate every example project
- `npm run clean` — remove build artifacts (`out/`, `build/`, `test-build/`, `*.tgz`)
- `npm run docs` — regenerate the JSDoc HTML site into `docs-site/` from the API doc comments plus the hand-written guides in `docs/` (config in `jsdoc.json`)
- `npm run docs-server` — build then serve the site locally via `scripts/docsServer.js`, which mirrors `docs-site/` under the same `opts.basePath` prefix (`/brighterscript-game-engine`) it's actually deployed at on GitHub Pages — serving `docs-site/` directly at a local server's root 404s every generated asset/nav link, since none of them are basePath-agnostic
- `npm run build-tests` — compile the Rooibos test suite (`bsconfig.test.json`) to `test-build/`
- `npm run test:ci` — build the tests, then run them headlessly via `brs-cli` (no device/simulator needed) — this is what CI runs
- `npm run test:device` — run the tests against a real device or brs-desktop (`ROKU_HOST`/`ROKU_PASSWORD` env vars), for interactive debugging

Working with the example apps (each example under `examples/<name>` is its own npm project with its own `package.json`/`bsconfig.json`, pulling in the engine via ropm):

- `npm run prepare-examples` — build the engine, then `npm install` in every example directory
- `npm run build-examples` — build the engine, then build every example (`bsc` + zip via `roku-deploy`) — output zips land in `examples/<name>/out/bge-<name>.zip`
- `npm run validate-examples` — validate the engine, then `bsc --validate --create-package=false` in every example
- `npm run clean-all` — clean the engine and every example
- `npm run create-example -- <name> ["Display Title"]` — scaffold a new `examples/<name>` from `scripts/exampleTemplate` (manifest, bsconfig, generated icon/splash images, minimal `MainRoom`), and register it in the root `.vscode/tasks.json` example picker
- `npm run create-entity -- <example> <EntityName>` — scaffold `examples/<example>/src/source/Entities/<EntityName>.bs` from `scripts/entityTemplate.bs` (a `BGE.GameEntity` subclass with `onCreate`/`onUpdate`/`onInput`/`onCollision` stubs)
- `npm run create-room -- <example> <RoomName>` — scaffold `examples/<example>/src/source/Rooms/<RoomName>.bs` from `scripts/roomTemplate.bs` (a `BGE.Room` subclass with `onCreate`/`onInput` stubs); still needs a manual `game.defineRoom(...)`/`game.changeRoom(...)` wired up in `main.bs` (and an entry in that example's `getRoomNames()`, if it has one)

All of the `*-examples` scripts fan out via `scripts/examples.js`, which iterates every `examples/*/` directory and runs the given command in each (a failure in one example doesn't stop the others, matching the original shell scripts this replaced). To act on a single example, `cd examples/<name>` and run its own npm scripts directly (`npm run build`, `npm run package`) instead. All `scripts/*.js` tooling is plain Node (no shell scripts) so it runs the same on Windows as macOS/Linux.

Beyond the Rooibos unit tests, confidence also comes from manually running an example on a real Roku or the Roku simulator via the VSCode BrightScript extension debug configurations in `.vscode/launch.json` / each example's own `.vscode/launch.json` (requires a `.env` with `ROKU_USERNAME`/`ROKU_PASSWORD`/`ROKU_HOST`, or the `Launch Simulator` config). To drive an example programmatically instead (sideload, launch, press keys, screenshot) — e.g. when asked to run, test, or "play" one — use the `rokubot` devDependency; see the `rokubot-examples` skill (`.claude/skills/rokubot-examples/SKILL.md`) for the workflow, speed tricks, and known per-example gotchas.

### Unit tests (Rooibos)

Tests are [Rooibos](https://github.com/rokucommunity/rooibos) v6 (`rooibos-roku`) suites, colocated next to the code they test as `*.spec.bs` (e.g. `src/source/math/Vector.spec.bs`, `src/source/utils/TagList.spec.bs`) — no separate `tests/` directory. `bsconfig.test.json` (extends `bsconfig.base.json`, loads the `rooibos-roku` plugin) builds them into `test-build/`; `bsconfig.build.json` excludes `*.spec.bs` entirely so none of this ships in a production build. `scripts/run-tests-ci.js` drives `brs-cli` (from the `brs-node` devDependency) headlessly for CI, watching stdout for Rooibos's `[Rooibos Result]: PASS`/`FAIL` line (the mocha reporter configured in `bsconfig.test.json` replaces, not supplements, the default console output) since `brs-cli` never exits on its own.

**Critical gotcha**: a `*.spec.bs` file may only contain **one** `@suite` class. Rooibos v6 (confirmed on `6.0.0-alpha.52`) silently corrupts test-suite metadata when two or more `@suite` classes live in the same file — tests run and report correctly for the classes processed first, then the run crashes with `[Rooibos Error]: ERROR RETRIEVING TEST SUITE DATA!!` while processing a later suite, regardless of which classes/content are involved. One class per file, always.

Constructing a real `BGE.Game` (which creates a real `roScreen`/`roCompositor`) inside a test works fine, including headlessly under `brs-cli` — confirmed via `Game.spec.bs`/`GameEntity.spec.bs`, which do this in `beforeEach`. Rooibos's own `stub()`/`mock()` can't fake native Roku components (`roScreen`, `roCompositor`, etc. — they only intercept methods on plain BrighterScript class/associative-array targets, and `CreateObject` itself can't be intercepted), so a real `Game` is the only way to exercise `GameEntity`/`Game` behavior; prefer it over duplicating engine logic by hand in a test. The same goes for `Renderer` (just needs a real bitmap, no `Game` required — see `Renderer.spec.bs`), colliders (needs a real `Game`/`GameEntity` for `setupCompositor`), and `Canvas`/`Room` (both need a real `Game`).

**`assertEqual` is type-strict** — `1` (Integer) and `1.0` (Float) fail against each other even though BrightScript would treat them as equal in most expressions. This bit almost every new spec file in this test suite. The type an assertion needs to match depends on how the value got there, not just its declared field type:
- A function param typed `as float`/`as integer` coerces the argument at the call boundary; a param typed `as dynamic` (common for optional/defaulted params) does not, so it carries through as whatever literal type was passed in.
- A class field typed `as float` does **not** get coerced on a later plain assignment (`m.x = 1` inside a method) — only `BGE.Math.VectorOps.create()`-style helpers that explicitly do `x * 1.0` reliably produce a `Float`.
- Some builtins matter too: `fix()`/`cint()` return `Integer`, `abs()` returns `Float`.
- When unsure, run the test once and read the actual/expected types out of the failure diff rather than guessing — Rooibos's assertion failure output prints the type of both sides.

**Avoid comparing whole objects with `assertEqual`** when they might embed circular references (e.g. a `GameEntity`/`Room` holding a `game` back-reference that itself holds the entity) or native Roku components (`roBitmap`, `roRegion`) — deep-equality comparison isn't guaranteed to handle either safely. Compare a distinguishing scalar field instead (an `id`, or a field you mutate specifically to mark identity, e.g. `renderer.frameCount = 12345` then assert the same value comes back through the code path under test).

### Docs site (`docs/`, `docs-site/`)

`docs/` holds hand-written developer guides (currently `game-engine-overview.md` and `engine-internals.md`, plus an `images/` folder) and is checked into git; `docs-site/` is the generated JSDoc HTML output (`npm run docs`) and is **gitignored, never committed** — the `docs.yml` GitHub Actions workflow builds it fresh and deploys it directly to GitHub Pages (via `actions/upload-pages-artifact`/`actions/deploy-pages`) on every push to `main`, no PR/review step and no need to hand-commit a regenerated site. GitHub Pages is configured as a `workflow`-type deployment (Settings → Pages → "GitHub Actions" as the source, not "Deploy from a branch") specifically so it can publish an arbitrary build directory like this without that directory living in the repo — the older "legacy" branch-deploy mode only allows a source path of `/` or `/docs`, which is why this repo moved off it. Site URL: `https://markwpearce.github.io/brighterscript-game-engine/` (a project-page subpath, not domain root, hence the `opts.basePath` config in `jsdoc.json`).

**`docs/` is published, so nothing internal goes there.** `opts.docs` is `./docs` with `opts.recurse: true`, so *every* markdown file anywhere under `docs/` is swept into the public site — a file without `group` frontmatter just gets appended to the sidebar alphabetically rather than being ignored. Design docs, specs, and implementation plans (e.g. from the superpowers skills) belong in `specs/` at the repo root instead: committed and reviewable, but outside the jsdoc tree.

Each guide in `docs/` is picked up via `jsdoc.json`'s `opts.docs` (a JSDoc `plugins/markdown`-rendered "docs" tree distinct from JSDoc's built-in `opts.tutorials` mechanism — this repo deliberately uses `opts.docs` instead, so guides fold into the same `sectionOrder`-driven sidebar as the API reference, e.g. `sectionOrder: ["Guides", "Namespaces", "Classes", "Interfaces"]`, rather than appearing as a bolted-on top-level menu link). Give each guide file YAML frontmatter:

```markdown
---
title: My Guide Title
group: Guides
order: 1
---
```

`group` controls which sidebar section it's clubbed under (must appear in `sectionOrder` to control placement, otherwise it's appended alphabetically); `order` controls position within that group. Cross-link between guides with a plain root-relative markdown link to the other guide's slug (its filename without extension, e.g. `[Engine Internals](/engine-internals)`) — the theme rewrites these through `opts.basePath` automatically, but literal `opts.menu` entries in `jsdoc.json` do NOT get this rewriting and need the `/brighterscript-game-engine` prefix added by hand. Diagrams must be flat SVG/PNG files under `docs/images/`, referenced with a normal markdown image tag — `clean-jsdoc-theme` does not support Mermaid code fences.

## Architecture

Everything lives in the `BGE` namespace. `rootDir` is `src`, `outDir` is `build` (see `bsconfig.json`).

### Game loop (`engine/Game.bs`)

`Game` is the top-level engine object (constructed once per app with canvas/UI dimensions). Its `Play()` method runs the single main loop, each frame:

1. **Input/event collection** — reads Roku universal control events, ECP input, audio events, and URL transfer events off message ports.
2. **Update** (`processEntitiesPreDraw`) — for every `GameEntity` (current room processed first, then `sortedEntities` in reverse): dispatch `onInput`/`onECPKeyboard`/`onECPInput`/`onAudioEvent`/`onUrlEvent`/`onUpdate`, then apply velocity to position (`processEntityMovement`). Once every entity has been processed, `Game.tweenManager` is ticked once (`BGE.TweenManager`, see `engine/TweenManager.bs`), writing any live tween's interpolated value onto its target field(s) — this runs after velocity integration, so a tween wins over velocity if both drive the same field that frame.
3. **Collisions** (`processEntitiesCollisions`) — per entity, checks each `Collider` against the `roCompositor`, and calls `onCollision(myCollider, otherCollider, otherEntity)` for every hit.
4. **Draw** (`drawEntities`) — calls `onDrawBegin`/`onDrawEnd` around a `Renderer.drawScene()` pass; the game canvas and UI canvas are rendered separately, then debug overlays.
5. **UI / Debug UI** — `gameUi` and `debugUi` (both `UiContainer` trees) get their own update+draw pass on a separate `uiCanvas`, drawn on top of the game canvas.
6. Swap buffers, run periodic garbage collection (`secondsBetweenGarbageCollection`), then apply a pending room change if `changeRoom()` was called this frame (room changes are deferred to end-of-frame via `roomChangedThisFrame`/`handleRoomChange`).

At every step, entities are re-checked for validity (`isValidEntity`) before each callback, since a callback may `Delete()`/invalidate the entity or trigger a room change mid-frame.

Entities are tracked in `Game.Entities` as `{ <entityName>: { <entityId>: GameEntity } }`, plus a flat `sortedEntities` array (draw/update order = zIndex order via insertion into this array). `Game.Statics`, `Rooms`, `Interfaces`, `Bitmaps`, `Sounds`, `Fonts`, `Models` are the other name-keyed asset/registry tables owned by `Game`.

### Entities, Rooms, Drawables (`engine/GameEntity.bs`, `engine/Room.bs`, `engine/drawables/`)

- `GameEntity` is the base class every game object extends. It exposes empty lifecycle hooks meant to be overridden: `onCreate`, `onUpdate`, `onCollision`, `onDrawBegin`/`onDrawEnd`, `onInput`, `onECPKeyboard`/`onECPInput`, `onAudioEvent`, `onUrlEvent`, `onPause`/`onResume`, `onChangeRoom`, `onGameEvent`, `onDestroy`. It owns `position`/`velocity`/`rotation`/`scale`, a `colliders` map, a `drawables` array, and a `tagsList` (`TagList`) for ad hoc tagging.
- `GameEntity.isOnScreen()` (aggregating `Drawable.isOnScreen()`, which aggregates `SceneObject.isCulled()`) reports whether an entity was actually drawn (not culled) last frame — a signal only, nothing in the engine skips an off-screen entity's own `onUpdate()`/collision checks automatically (a culled entity can still matter for gameplay, e.g. approaching from off-screen), so use it to opt in to skipping your own purely-visual per-frame work. See issue #75.
- **Two color formats, and they're easy to mix up**: `Drawable.color`/`Drawable.outlineRGBA` take packed **RGB** (`0xRRGGBB`, named colors in `BGE.ColorsRGB`) while every `Renderer.draw*` call takes packed **RGBA** (`0xRRGGBBAA`, named colors in `BGE.Colors`). Passing one where the other is expected gives a plausible-looking wrong color, not a type error. Note `BGE.getRandomColorRGB()` returns the RGB form (for drawables) and `getRandomColorRGBA()` the RGBA form.
- `onInput(input as GameInput)` (`engine/GameInput.bs`) reports directional `x`/`y` in **world space** (standard math convention, +y is up) — pressing "up" gives `y: 1`, "down" gives `y: -1`. Only the renderer's final world-to-canvas/raster projection (see `Camera2d.worldPointToCanvasPoint`) flips y to screen/pixel space (+y down). Every example that does `velocity.y = input.y * speed` relies on this.
- `Room` (in `engine/Room.bs`) is itself just a `GameEntity` subclass — the "current room" is processed like any other entity but always first/last in update and collision passes. Room changes go through `Game.changeRoom()`/`defineRoom()`, and non-persistent entities are destroyed on room change.
- `Drawable` (`engine/drawables/Drawable.bs`) is the base class for visual attachments on a `GameEntity` (`Image`, `Sprite`, `AnimatedImage`, `DrawableRectangle`, `DrawableLine`, `DrawablePolygon`, `DrawableText`, `DrawableCircle`, `DrawableSphere`, `Model3d`, `DrawablePlane`, `DrawableParticles`). Each drawable computes its own transformation matrix from offset/rotation/scale and, via `addToScene(renderer)`, registers a `SceneObject` with the `Renderer` — the actual per-frame draw call happens in the renderer, not the drawable. See [Drawables and SceneObjects](docs/drawables-and-scene-objects.md) for an in-depth guide to each drawable/scene-object pair and how the renderer draws them.
- `DrawableCircle`'s fill is a texture blit of `Renderer.getCircleResource()` — a circle rasterized once (lazily, on first use) and shared by every circle/sphere in that renderer, not a shape rasterized per-instance — while its outline is a separate N-gon (`outlineSegments`) inscribed in the object's own quad, computed fresh each frame through the same generic `getOutlineCanvasPoints()` hook `SceneObjectPolygon` uses. `DrawableSphere` is a `DrawableCircle` that forces `drawMode = directScaled` in its own constructor so it never foreshortens, regardless of camera angle — see `SceneObject.getActualDrawMode()`, which only resolves `matchCamera` through the camera and passes any other explicit value straight through, so no separate `SceneObjectSphere` class exists. `SceneObjectCircle` also opts `solid`/`solidDrawBackFace` into the oriented modes' temp-bitmap caching by overriding `usesTempBitmap()` since its fill is a real texture warp there too, unlike `DrawableRectangle`'s cheap flat quad fill that mode was written for - unless `BGE.Math.CornerPoints.isApproximatelyRotatedRectangle()` finds this frame's projected quad is (within tolerance) a plain rotation of a rectangle, in which case `usesTempBitmap()` returns `false` for that frame instead and `drawToCanvas()` blits the circle via `SceneObjectBillboard.drawRegionAsRotatedQuad()` (one native `DrawTransformedObject` call, the same helper `directScaled` uses) rather than the exact `drawPinnedCorners` triangle warp - correct for a disc rotated only about the axis facing the camera (any in-plane angle, including none), and measured ~1600x cheaper per call on real hardware than the exact path it replaces (issue #105, `examples/rendererTest`'s `circle-fast-draw-benchmark` demo). A quad skewed by perspective foreshortening (tilted on another axis) fails that check and falls back to the exact path, unchanged from before.
- **Outlines** are shared across every billboard drawable: setting `Drawable.outlineRGBA` (packed RGB, `invalid` = no outline — there's no separate flag) makes `SceneObjectBillboard` stroke an outline along the object's canvas corner points on top of the fill, with `outlineWidth` for thickness. A `SceneObject` whose shape isn't a single quad opts out by returning `invalid` from `getOutlineCanvasPoints()` (`SceneObjectModel` does; `SceneObjectPolygon` overrides it to use its own point list). The `wireFrame` draw modes stroke the outline themselves in `drawToCanvas` and are skipped by the hook, so they don't get stroked twice.
- **A drawable that changes shape in place** (e.g. `DrawableRectangle.setSize`) must call `Drawable.invalidateGeometry()` — a resize isn't movement, so the `MotionChecker` dirty-checking can't see it. That bumps a `geometryVersion` which `SceneObject.geometryChanged()` compares against, so world/canvas geometry is recomputed even though nothing moved. It's a version number rather than a flag specifically so one `Drawable` registered with two `Renderer`s (game canvas + UI canvas) can't have one SceneObject clear the flag before the other saw it.

### Renderer / SceneObjects (`engine/renderer/`)

- **Cull latching**: a `SceneObject` that the camera frustum rejects latches (`SceneObject.isCulled()`) so a stationary off-screen object doesn't re-run the frustum check every frame — only a genuine cull latches, not an attempted-but-failed draw, or a single bad frame becomes permanent (issue #48). That latch also covers a *deterministic* draw failure — one that depends only on this object's own position/orientation relative to the camera and canvas, so it'll keep failing every frame until something moves, same as a frustum cull. `SceneObject.isDeterministicDrawFailure()` is the extension point (default `false`, meaning "could be transient, keep retrying"); `SceneObjectBillboard` overrides it to cover a backface cull and an in-frustum-but-entirely-off-canvas rejection, both of which used to silently re-run their own (cheap but non-zero) checks every single frame with no latch at all (issue #73).
- **Depth sort** (issue #59): `Renderer.drawScene()` skips its per-frame `sortBy` entirely when nothing that affects order changed that frame (`needsDepthSort`, set by `SceneObject.update()`'s existing movement/camera dirty-check plus `addSceneObject`/`removeSceneObject`, read via `Renderer.didSortLastFrame()`), and breaks ties between depths that quantize into the same `DEPTH_TIE_EPSILON`-wide bucket by the previous frame's relative order (`SceneObject.stableSortKey`/`getLastSortIndex`) rather than whatever `roArray.SortBy` happens to produce for jittering floats — this is bucket quantization (`Int(depth / DEPTH_TIE_EPSILON)`), not a true epsilon-tolerance comparison, so two depths closer together than the epsilon can still straddle a bucket boundary and swap order — this is still a whole-object painter's sort, just a cheaper and more stable one, not new visibility behavior. Separately, `BGE.DepthSort` (`renderer/DepthSortHelpers.bs`) computes per-frame **overlap clusters**, gated behind `Renderer.computeOverlapClusters` (off by default): a broad-phase **sort-and-sweep** (`sortByMinXIncremental`/`sweepForCandidatePairIndexes` — an incremental insertion-sort by screen-space minX plus an active-set sweep, replacing an original all-pairs `O(n^2)` scan that measured a ~70% FPS regression at ~176 scene objects on real hardware) followed by a narrow-phase SAT test (`hullsOverlap`) rejects false positives like a long diagonal wall's AABB spuriously overlapping a nearby-but-separate model; surviving pairs union-find into clusters via `groupIntoClusters()`, exposed read-only as `Renderer.getOverlapClusters()`. A `SceneObject` opts out of candidacy entirely via `participatesInOverlapDetection()` (default `true`; `SceneObjectLine` overrides `false` — a 2-point line hull can never pass the narrow phase's hull-validity check anyway, so this is pure broad-phase cost avoidance for a computation guaranteed to reject it, not a tolerated visual approximation). A multi-member cluster's members draw via `getPrimitiveCount()`/`getPrimitiveDepth(index)`/`drawPrimitive(rendererObj, index)` (base-class defaults: 1 primitive, this object's own depth, delegates to the normal cached `performDraw()` — correct as-is for every billboard type, since a billboard is always one piece regardless of clustering) instead of the normal whole-object `draw()` call; `Renderer` collects every deferred cluster member's primitives into one combined list, sorts it once by depth, and draws interleaved (`addPendingClusterDraw`/`drawPendingClusterPrimitives`). `SceneObjectModel` overrides all three to expose its per-face list (`m.modelCanvasFaces`, already rebuilt per-frame by `updateCanvasPosition()` reflecting the current draw mode's backface culling) and draws each face directly via a shared `drawFaceToCanvas()` helper, bypassing its own whole-model temp-bitmap cache entirely (that cache aggregates faces in an order that can't represent cross-object interleaving). A solo cluster (no other object's bounds overlap it) draws exactly as before — zero behavior/perf change for the common case. See `specs/2026-08-15-depth-sort-design.md` and `specs/2026-08-16-depth-sort-plan-2-design.md` for the full design (including why this isn't BSP — that's issue #107 — and why occlusion culling is a separate future issue, #108) and `examples/depthsort` (`TieBreakRoom`, `ClusterVisualizerRoom`) for a runnable demo.
- `Renderer` (`renderer/Renderer.bs`) wraps an `ifDraw2D` surface (a `Canvas`'s bitmap) and owns the list of `SceneObject`s to draw each frame, plus a `Camera` (`Camera2d` or `Camera3d`, under `renderer/cameras/`), a `TriangleCache` (caches rasterized triangle bitmaps — triangle drawing is otherwise expensive), and a `ScratchBitmapPool` (`ScratchBitmap.bs`) for reusable off-screen bitmaps. There is one `Renderer` for the game canvas and a separate one for the UI canvas (`Game.canvas.renderer` / `Game.uiCanvas.renderer`). `Renderer.resources` holds its own one-off generated assets (`rightTriangle`, `circle`) — `circle` is built lazily via `getCircleResource()`/`getCircleRegionWithId()` on first use rather than eagerly for every `Renderer`, since most never draw a circle at all and rasterizing one is expensive enough to matter across the whole test suite's worth of `Game` constructions. `Renderer.drawCircle()`/`drawCircleTo()` expose this as a standalone draw call (scales the circle resource via `drawScaledObjectTo`), the same way `drawRectangle()` wraps a native `DrawRect`.
- `SceneObject` subclasses (`sceneObjects/SceneObject*.bs`) represent what actually gets drawn: `SceneObjectImage`, `SceneObjectBillboard`, `SceneObjectLine`, `SceneObjectPolygon`, `SceneObjectRectangle`, `SceneObjectText`, `SceneObjectCircle`, `SceneObjectModel`, `SceneObjectSkybox`, `SceneObjectPlane`, `SceneObjectParticle`. `SceneObjectParticle` is the one documented exception to "one `SceneObject` per `Drawable`" in this codebase — a single instance draws its whole `DrawableParticles` emitter's live particles in one batched loop, specifically to avoid `addSceneObject`/`removeSceneObject` churn defeating depth-sort's skip-optimization during continuous emission; see `specs/2026-08-18-particle-system-design.md`. Each `SceneObjectDrawMode` (`matchCamera`, `directToCamera`, `directScaled`, `oriented`/`orientedDrawBackFace`, `wireFrame`/`wireFrameDrawBackFace`, `solid`/`solidDrawBackFace`) controls how an object reacts to camera rotation/perspective — this is what gives the 2D-renderer-based engine its pseudo-3D/billboard capability (see `examples/3d`). A texture-backed billboard (`SceneObjectImage`, `SceneObjectCircle`) pays a real per-pixel warp cost (`drawPinnedCorners`) in the oriented/solid modes that a flat-fill one (`SceneObjectRectangle`, `SceneObjectPolygon`) doesn't — this is proportional and expected, not a bug, confirmed by comparing `examples/3d`'s `RectanglesRoom` against `CirclesRoom` on a simulator (where oriented-mode rendering in general is markedly slower than on real hardware). `SceneObjectCircle` skips that cost whenever it safely can — see the `isApproximatelyRotatedRectangle` bullet above (issue #105).
- `SceneObjectPlane`/`DrawablePlane` render a textured ground/floor plane (Mode-7-style: the camera's view frustum is projected onto the plane, then the resulting quad is warped back into perspective-corrected horizontal slices) — see `examples/terrain`. By default (`fillMode: staticImage`, see below) it's a **finite decal, not an infinitely tiling plane** despite the "infinite plane" doc comment on `DrawablePlane` — the texture's center pixel is anchored on the plane's own world position (`BGE.Math.worldPointToTexturePixel`), and driving/looking far enough away from that anchor correctly shows nothing (the renderer's background) rather than wrapping/tiling. True tiling now exists as a separate `fillMode: tiledImage` option (see below) rather than being a follow-up. Two gotchas if you touch this code: (1) `ScratchBitmapPool.getRegion()` hands out **pooled, reused bitmap memory that is not cleared** — `SceneObjectPlane.populatePerspectiveBmp()` must clear both its scratch region and `prePerspectiveBmp` before drawing into them, or stale pixels from a previous, unrelated frame's use of that same pooled bitmap show through (this looked exactly like the texture "wrapping/repeating" before it was tracked down — it wasn't a sampling-wrap bug at all). (2) each slice's screen position comes from projecting its two world-space boundary points through the camera's own perspective formula (`projectPlanePointToCanvasY`, the same math `Camera3d.worldPointToCanvasPoint` uses for every other 3D object) and rounding to whole pixels, so adjacent slices' rounded positions can disagree by a fraction of a pixel and leave a thin black seam between them — drawing each slice a couple of pixels taller than its exact band (`seamPadding`) papers over this. This plane's vertical positioning is computed via true perspective projection (matching every other 3D object exactly), not the arbitrary easing curve it used before — replacing that curve is what fixed both a ground-density mismatch under roll and a general ground/billboard alignment issue during plain camera movement. See [Drawables and SceneObjects](docs/drawables-and-scene-objects.md) for the full walkthrough. `Camera3d.rollDegrees` (rotation about the camera's own forward axis) is supported throughout - ordinary 3D objects (billboards, models) bank correctly because they're projected per-point through the camera's view matrix, but `SceneObjectPlane` can't do that (it rasterizes via horizontal trapezoid slices, which can't represent a tilted horizon directly) - instead it computes its geometry against an *unrolled* view enlarged to the frame's diagonal, then rotates the composited result by the roll angle before cropping it to the frame. See `specs/2026-08-19-camera-roll-and-plane-horizon-design.md`.
- `Camera3d.maxDrawDistance` (default `1000`, world units) is the shared far-clip every `SceneObject.isInView` check now honors, not just `SceneObjectPlane` — a point farther than this in front of the camera is treated as out of view everywhere in the renderer. `SceneObjectPlane` derives both its own far-render-distance and (see next bullet) its tiled-supertexture size from this same field instead of a private constant, so raising/lowering it changes how far the ground is drawn without any plane-specific code (issue #124). The effective value is silently capped per-device, re-applied every frame via `checkMovement()`: 900 on the BrightScript Simulator (`Camera3d.getMaxDrawDistanceDeviceCap()`, confirmed by binary search against `lvcabral/brs-engine#1198`); FHD real hardware keeps the full default (a 5000 placeholder cap, not yet confirmed on real FHD hardware - issue #142). SD/HD real hardware is capped at 30, confirmed by binary search on a real Roku Express (model 3930, 512MB RAM - issue #137): past that, the same class of scratch-bitmap-allocation failure #142 investigates on FHD hits multiple `SceneObjectPlane` bitmaps at once on this far more memory-constrained tier (60 confirmed working, 65 confirmed crashing - 30 leaves real margin below that razor-thin boundary, and FPS was already mediocre, ~31-32fps, at 50-60 on this device). **This cap only prevents that one crash - it does not mean `examples/terrain` renders correctly on SD/HD hardware.** The same real device hit two other failures independent of `maxDrawDistance` entirely (confirmed still failing even at `maxDrawDistance=1`): `SceneObjectSkybox`'s own plain 1280x720 temp bitmap fails to allocate outright (crashes `WorldRoom` unconditionally), and `SceneObjectPlane`'s perspective-slice `drawScaledObjectTo` calls silently fail on this hardware whenever a slice needs a large scale factor, so the ground never actually draws even where nothing crashes. See issue #151. Reading `maxDrawDistance` back after setting it higher than the device cap returns the capped value, not what was set.
- `DrawablePlane.fillMode` (a `BGE.PlaneFillMode`: `color`, `tiledImage`, `staticImage`) picks how a plane's surface is filled — `color` is a flat fill from `Drawable.color`/`alpha` with no texture at all (never "runs out," so it's the natural base/backdrop layer), `tiledImage` treats `region` as a single repeating tile seamlessly covering the ground out to `maxDrawDistance`, and `staticImage` (the default, and the only mode that existed before issue #53) is the finite decal described above. Multiple `DrawablePlane`s in any mix of modes compose on the same entity/room via ordinary `addDrawable()` calls — draw order for planes at the same depth follows insertion order (the same depth-sort tie-break every `SceneObject` uses), so add the base color layer first, then tiled/static layers on top. See `examples/terrain` for a runnable stack of all three.
- `tiledImage`'s cached "supertexture" bitmap (built lazily on first draw, rebuilt only if `maxDrawDistance` changes afterward, one per `DrawablePlane` instance — not shared across instances) tiles `region` across a grid roughly `2 * maxDrawDistance / tileSize` tiles per axis, so its tile count (and build cost) scales with `(maxDrawDistance / tileSize)²` — pick a tile size (and consider `maxDrawDistance`) with this in mind, since a small tile with a large `maxDrawDistance` can build a very large bitmap.
- `examples/terrain`'s entry point is `WorldRoom` (a free-fly camera over a billboard-tree-dotted ground plane); `MainRoom`, reachable from there via Back, demonstrates the three `fillMode`s (`color`/`tiledImage`/`staticImage`) side by side.
- `BGE.FreeFlyCameraController` (`engine/controllers/FreeFlyCameraController.bs`) is a reusable, non-`GameEntity` free-fly camera control scheme (yaw/pitch relative to the camera's current roll-adjusted orientation, forward/back drive, roll, ground-plane clamp) promoted from `examples/terrain`'s original hand-rolled controller (issue #148) — construct one with a `Camera3d` and a `BGE.Math.Plane`, then forward a `Room`/`GameEntity`'s own `onInput`/`onUpdate` calls to it. It's deliberately generic: `examples/terrain`'s `TerrainFreeFlyCameraController` subclasses it to add that example's own Back-button room-switching, hold-to-toggle debug overlay, and hint text, none of which belong in the engine.
- `SceneObjectSkybox` renders in its own earliest pass in `Renderer.drawScene()` (before planes and all other objects), wrapping a panoramic texture around the camera; it uses the same render-then-rotate roll-handling trick as `SceneObjectPlane` (see `specs/2026-08-19-camera-roll-and-plane-horizon-design.md` section D) and never participates in depth-sort or overlap detection — see issue #65.

#### Manually exercising the Renderer (`examples/rendererTest`)

`rendererTest` is the place to try out or demonstrate a `Renderer`/`ifDraw2D` capability directly — deliberately **not** built on `BGE.Game`/`Room`/`GameEntity`, since the point is to exercise `Renderer` (and, if needed, raw `roScreen`/`roBitmap`/`roRegion` calls) in isolation, the same way a consumer might before ever touching the rest of the engine. `main.bs` owns a raw `roScreen` + message port and a `BGE.Renderer` built directly from it (`new BGE.Renderer(screen)`, no `Game` involved), and runs an on-screen menu grouped by category (Up/Down to select, OK to run, Back to return to the menu).

**To add a new demo**: create a class under `examples/rendererTest/src/source/Tests/` extending `RendererTest` (`RendererTest.bs` — override `setup(renderer)` for one-time setup, `update(dt)`/`draw(renderer)` for the per-frame work, `onInput(buttonName)` for anything beyond Back, which `main.bs` already handles), then add one entry to the array in `DemoList.bs` (`{id, category, name, create: function() as RendererTest ... }`). That's the only file that needs editing — the menu picks it up automatically, grouped under `category`.

**Every demo is timed automatically.** `main.bs` times each demo's `update()`/`draw()` passes and displays a `FpsDisplay`-style line under it (`FrameTimer.bs`, averaged over 30 frames): fps, frame ms, update ms, draw ms, and the renderer's draw-call count for the frame (`Renderer.getDrawCallsLastFrame()`). No demo has to opt in. This matters because **Roku draw costs differ by more than an order of magnitude between operations** and are not predictable by reading the code — a `DrawRect` or plain bitmap blit runs at ~16-17k/sec while rasterizing a rotated quad manages ~500/sec (measured; see the [roku-draw2d-performance](https://github.com/markwpearce/roku-draw2d-performance) benchmark channel for per-operation numbers). Never assert one draw path is cheaper than another — measure it. The `quad-fill-benchmark` demo is the worked example of timing two specific approaches against each other in a single run (it's what established that skipping `drawPolygon`'s convex-hull pass for a known-order quad is worth only 4-7%, while caching the fill into a bitmap is worth ~30x). `circle-fast-draw-benchmark` is a second worked example of the same pattern (exact `drawPinnedCorners` vs. `SceneObjectCircle`'s rotate+scale fast path, issue #105) - it deliberately uses a small iteration count (15, not the 60x5 `quad-fill-benchmark` uses) because the exact path alone measured ~110ms/panel on real hardware, and a larger count blocking inside `setup()` tripped the Roku OS's "app not responding" watchdog, silently killing and relaunching the channel mid-benchmark - keep a new benchmark's total blocking time in `setup()` well under that if the operation under test is this expensive per call.

Two gotchas hit while building this, worth knowing before touching this file:
- The main loop must read input via `port.GetMessage()` (non-blocking), **not** `wait(0, port)` — in BrightScript, a `wait()` timeout of `0` means "wait forever" (no timeout), not "don't wait"; using it here froze the whole app on the splash screen until the first button press.
- `Renderer.drawPinnedCorners()`/`drawBitmapTriangleTo()` are relatively expensive per call (they check out/rasterize scratch bitmaps internally) — a demo that draws many of them in a loop should render once into a cached bitmap (in `setup()`) and blit that each frame, rather than redoing the full loop every frame at 60fps. `CornerPinGridTest.bs` does this; doing it the naive way caused most of a 26-tile grid to silently fail to render after the first few tiles.

A demo can also be launched directly, skipping the menu, via a `demo` deep-link param matching a `DemoList` entry's `id` — e.g. `rokubot launch dev --param demo=triangles` (see the `rokubot-examples` skill for the general `rokubot` workflow). Handy for scripted checks of one demo at a time instead of navigating the menu by hand.
- `Canvas` (`engine/Canvas.bs`) pairs a bitmap with a `Renderer` and scale/offset, and is the thing `Game` composites to the physical `roScreen` each frame (game canvas and UI canvas are composited independently, letting UI stay crisp regardless of game canvas scaling).

### UI (`engine/ui/`)

`UiContainer`/`UiWidget`/`Label`/`Style`/`Alignment` implement a small retained-mode widget tree, drawn to its own canvas layer above the game world. `Game.gameUi`/`Game.debugUi` are the two top-level containers; debug windows (`engine/debug/`: `DebugWindow`, `FpsDisplay`, `GarbageCollectorDisplay`, `MemoryDisplay`, `InputDisplay`, `LogDisplay`) attach under `debugUi` and are toggled via `Game.debugShowUi()`/`debugDrawColliders()`/etc. `Game.enableStandardDebugUi(args = {})` wires up all five of these in one call (pass e.g. `{log: false}` to skip one) instead of adding each by hand.

### Collision (`engine/colliders/`)

`Collider` (base), `CircleCollider`, `RectangleCollider` wrap a Roku `roCompositor`/`roSprite` region per entity; collision checks happen through the compositor's `CheckMultipleCollisions()`, not manual AABB math.

### Math / Utils

`math/` has the vector/matrix/transform primitives engine code is built on (`Vector`, `Matrix44`, `Transform`, `CornerPoints`, `Triangle`, plus 2D `circle`/`rectangle` helpers) — `BGE.Math.VectorOps` and `BGE.Math.Matrix44` are used pervasively rather than raw associative arrays. `utils/` has cross-cutting helpers: `quickhull` (convex hull, used for 3D model face computation), `STLParser`/`OBJParser` (load `.stl`/`.obj` 3D models into the same `BGE.Model3d`, see `Game.load3dModel`), `tweens`, `MotionChecker` (dirty-checking for transforms, avoids recomputing matrices for unmoved objects — see its use in `Drawable.computeTransformationMatrix`), `TagList`, `registry` (Roku registry read/write helpers), `colors`, `strings`.
- **`OBJParser` supports geometry and one diffuse texture per model** (issue #89): it parses `.obj` `v`/`vt`/`f` lines (fan-triangulating any quad/n-gon face, including each face's UV points) into `Model3dFace`, computing each face's normal from its own vertex geometry rather than trusting `vn` data (`Model3dFace` only supports one flat normal per face, not smooth/per-vertex shading). A texture comes from either `mtllib`/`map_Kd` (resolved relative to the `.obj`'s own directory) or an explicit `options.texturePath` passed to `Game.load3dModel` (which always wins) - `usemtl`/multi-material meshes are still out of scope, one texture per model. `BGE.Model3dOps.applyTexture` does the actual bitmap-load-to-pixel-space-UV finalization; `SceneObjectModel` draws a textured face via `Renderer.drawBitmapTriangle(To)` (affine-mapped, not perspective-correct) instead of a flat fill. See `specs/2026-08-19-textured-obj-models-design.md`.

## Conventions specific to this codebase

- `bslint.json` disables `named-function-style` and `eol-last`, sets `inline-if-style: never` (don't write single-line `if`), and allows implicit type annotations.
- Public API surface is documented with JSDoc-style `'` comments (`@param`, `@return`) directly above class methods — these are pulled into the generated docs (`npm run docs`) via `brighterscript-jsdocs-plugin`; follow this style for any new public engine method.
- Internal-only state on `Game` is fenced with `' ****BEGIN - For Internal Use, Do Not Manually Alter****` / `****END...****` comments — treat that block as engine-private even though BrighterScript doesn't enforce it.
- Because entity callbacks can invalidate/delete the entity (or trigger a room change) mid-callback, engine code re-validates entities with `isValidEntity()`/`m.isValidEntity()` after every single callback invocation — follow this pattern when touching the per-frame processing methods in `Game.bs`.
- **`=` between two custom-class instances is a runtime crash, not a compile error, and an `as object`/`as dynamic` cast does not fix it — and this isn't limited to custom classes.** BrightScript's `=` operator doesn't support reference-equality comparison between two `roAssociativeArray`-backed class instances (a `Type Mismatch` error at runtime) — the BrighterScript type-checker doesn't catch this at compile time either, so `if sceneObj.drawable = m.panel` type-checks cleanly and only crashes the first time that line actually runs. Casting one side `as object`/`as dynamic` silences the type-checker but changes nothing about what the underlying VM's `=` operator supports, so it does *not* fix the crash — confirmed twice now (once with two custom-class instances during #59's development, only surfacing on an actual on-device run rather than `npm run check`/`bsc --validate`; once with two `roRegion` instances during #124/#53's development, this time caught by the Rooibos suite itself with the exact same `Type Mismatch. Operator "=" can't be applied to "Object" and "Object"` message). Assume this applies to any native `ifDraw2D`-family component (`roRegion`, `roBitmap`, etc.), not just custom classes. Compare a stable identity field instead — `SceneObject.id` (a string) via `Drawable.getSceneObjects()`, an entity's own `id`/`name`, or a scalar proxy like dimensions for a native drawing object — never the object reference itself.
- **`GameInput` fires once for press and once again for release of the same physical button** (`GameInput.press`/`.held`/`.release`, see the button-code-offset table on the class). An `onInput` check like `else if input.isButton("fastforward")` with no `input.press` guard runs the action twice per button tap (once on press, once on release) — for a toggle or a short back-and-forth cycle (e.g. switching between exactly two rooms) this looks exactly like "nothing happened," not like a double-fire, since the two calls cancel out. Always guard a discrete/one-shot action with `input.press and input.isButton(...)`, matching the existing convention on the `"back"` check every room's `onInput` already uses.
- **Automated Rooibos tests do not exercise example apps' own entity/room code at all** — they cover engine internals only. A crash or logic bug confined to `examples/*` (as opposed to `src/source/engine/`) will pass `npm run check` and `npm run validate-examples` cleanly and still crash the instant it's actually sideloaded and run — both `=`-comparison-crash bullets above were found this way, not by any automated check. Treat an actual on-device (or simulator) run via the `rokubot-examples` skill as mandatory before considering a new/changed example's runtime behavior verified, not as optional polish once static analysis is clean.
