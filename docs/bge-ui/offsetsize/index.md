---
title: OffsetSize
kind: class
longname: BGE/UI.OffsetSize
---

# OffsetSize

**Properties**

- `top` (float)
- `right` (float)
- `bottom` (float)
- `left` (float)

---

## Constructor

<Signature
  code="new OffsetSize(
	tOffset?: float,
	rOffset?: dynamic,
	bOffset?: dynamic,
	lOffset?: dynamic,
): OffsetSize"
/>

**Parameters**

- `tOffset` (float, optional, default: 0)
- `rOffset` (dynamic, optional, default: "invalid")
- `bOffset` (dynamic, optional, default: "invalid")
- `lOffset` (dynamic, optional, default: "invalid")

---

## Instance Methods

<MemberHeading
  id="set"
  depth="3"
  name="set"
  sig="set(
	tOffset?: float,
	rOffset?: dynamic,
	bOffset?: dynamic,
	lOffset?: dynamic,
): void"
/>

**Parameters**

- `tOffset` (float, optional, default: 0)
- `rOffset` (dynamic, optional, default: "invalid")
- `bOffset` (dynamic, optional, default: "invalid")
- `lOffset` (dynamic, optional, default: "invalid")

**Returns**

- `void`
