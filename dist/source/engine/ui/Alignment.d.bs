namespace BGE.UI
    enum HorizAlignment
        left = "left"
        center = "center"
        right = "right"
    end enum
    enum VertAlignment
        top = "top"
        center = "center"
        bottom = "bottom"
    end enum
end namespace
