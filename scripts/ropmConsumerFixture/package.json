{
    "name": "bge-ropm-consumer-fixture",
    "version": "1.0.0",
    "private": true,
    "description": "Throwaway consumer project used by scripts/test-ropm-consumer.js to prove `ropm install` actually works for a real downstream consumer of this engine. The `dependencies` field is intentionally empty here - the script injects a `file:` pointer to a freshly-packed tarball at runtime, so this checked-in copy never needs to change.",
    "devDependencies": {
        "brighterscript": "1.0.0-alpha.52",
        "ropm": "^0.11.9"
    },
    "dependencies": {},
    "ropm": {
        "rootDir": "src",
        "noprefix": [
            "brighterscript-game-engine"
        ]
    }
}
