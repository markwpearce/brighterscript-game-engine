class __CLASS_NAME__ extends BGE.GameEntity

  sub new(game as BGE.Game)
    super(game)
    m.name = "__CLASS_NAME__"
  end sub

  override sub onCreate(args as roAssociativeArray)
    ' Add drawables/colliders here, e.g.:
    ' bitmap = m.game.getBitmap("...")
    ' region = CreateObject("roRegion", bitmap, 0, 0, bitmap.GetWidth(), bitmap.GetHeight())
    ' m.addImage("sprite", region)
    ' m.addCircleCollider("body", bitmap.GetWidth() / 2)
  end sub

  override sub onUpdate(deltaTime as float)
  end sub

  override sub onInput(input as BGE.GameInput)
  end sub

  override sub onCollision(myCollider as BGE.Collider, otherCollider as BGE.Collider, otherEntity as BGE.GameEntity)
  end sub

  ' Other lifecycle hooks you can override: onDrawBegin/onDrawEnd, onECPKeyboard/onECPInput,
  ' onAudioEvent, onUrlEvent, onPause/onResume, onChangeRoom, onGameEvent, onDestroy.
  ' See BGE.GameEntity for their signatures.

end class
