namespace BGE.Math

  class Transform
    position as BGE.Math.Vector = new Vector()
    rotation as BGE.Math.Vector = new Vector()
    scale as BGE.Math.Vector = new Vector(1)
  end class

end namespace