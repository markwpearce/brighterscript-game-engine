{
  "private": true,
  "scripts": {
    "build": "bsc",
    "clean": "rimraf dist && rimraf out",
    "package": "bsc --create-package"
  },
  "ropm": {
    "rootDir": "src"
  },
  "devDependencies": {
    "brighterscript": "../../../brighterscript",
    "rimraf": "^3.0.2",
    "ropm": "^0.10.4"
  }
}
