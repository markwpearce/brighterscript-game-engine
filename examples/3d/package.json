{
  "private": true,
  "scripts": {
    "build": "bsc",
    "clean": "rimraf dist && rimraf out",
    "package": "bsc --create-package"
  },
  "ropm": {
    "rootDir": "src"
  },
  "devDependencies": {
    "brighterscript": "^0.66.0-alpha.6",
    "rimraf": "^3.0.2",
    "ropm": "^0.10.4"
  }
}
