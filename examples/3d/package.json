{
  "private": true,
  "scripts": {
    "build": "echo \"HELLO WORLD\" && bsc",
    "clean": "rimraf dist && rimraf out",
    "package": "bsc --create-package"
  },
  "ropm": {
    "rootDir": "src"
  },
  "devDependencies": {
    "brighterscript": "^1.0.0-alpha.36",
    "rimraf": "^3.0.2",
    "ropm": "^0.10.4"
  }
}
