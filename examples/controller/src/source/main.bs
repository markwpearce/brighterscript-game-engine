sub Main()
  game = new BGE.Game(1280, 720) ' This initializes the game engine
  game.fitCanvasToScreen()
  game.enableControllerInput()

  ' Player 1 (playerIndex 0): remote OK or controller button "a" fires; move
  ' with the remote d-pad or the first connected browser's stick "1".
  game.controls.bindAction("fire1", "ok", "a", 0, "Fire")
  game.controls.bindAxis("move1", "1", 0, "Move")

  ' Player 2 (playerIndex 1): a second connected browser - no remote binding,
  ' since there's only one physical Roku remote.
  game.controls.bindAction("fire2", invalid, "a", 1, "Fire")
  game.controls.bindAxis("move2", "1", 1, "Move")

  firstRoom = new MainRoom(game)
  game.defineRoom(firstRoom)
  game.changeRoom(firstRoom.name)

  game.enableStandardDebugUi({memory: false, garbageCollector: false})

  game.play()
end sub
